# MySQL 通用备份脚本环境变量示例（复制为 .mysql_backup.env 后修改）
# 变量前缀统一为 MYSQL_*

MYSQL_MODE=auto                   # 设为 mysql5/mysql8 强制模式，或 auto（默认）通过 mysql --version 自动检测
MYSQL_USER=backup
MYSQL_PASSWORD=Backup!ChangeMe
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_CONF_FILE=/etc/my.cnf
MYSQL_TOOL_HOME=/usr             # XtraBackup 安装位置，默认 /usr
# MYSQL_TOOL_BIN=/usr/local/bin  # 如需覆盖完整 bin 目录
MYSQL_TARGET_DIR=/data/mysql_backups
MYSQL_FULL_PREFIX=full
MYSQL_INCREMENT_PREFIX=incr
MYSQL_FULL_WEEKDAY=7             # 周日全量
MYSQL_FORCE_FULL=false
MYSQL_DATA_DIR=/data/mysql_backup_runtime
MYSQL_LOG_DIR=/data/mysql_backup_runtime/log
MYSQL_VAR_DIR=/data/mysql_backup_runtime/var
MYSQL_INDEX_FILE=/data/mysql_backup_runtime/var/mysql_increment.index
MYSQL_ERROR_FILE=/data/mysql_backup_runtime/var/mysql_increment.err
MYSQL_INDEX_BACKUP=true
MYSQL_LOG_RETENTION=30
